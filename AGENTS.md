# Attunement Monthly Observatory

Agent documentation lives in `CLAUDE.md` (single source of truth: mission,
learning contract, invariants, and the monthly loop).

Skills live in `.claude/skills/`. The entry skill for monthly runs is
`orchestrator`. `outlook-generator` and `instrument-review` are manual,
human-gated skills.
