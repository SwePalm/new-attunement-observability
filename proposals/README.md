# Proposals

Output of `instrument-review` (quarterly, manual). Each file proposes skill
diffs with evidence, expected effect, and drift risk. A human merges (or
declines) and bumps METHOD_VERSION in CLAUDE.md on merge. Agents never
apply these changes themselves.
